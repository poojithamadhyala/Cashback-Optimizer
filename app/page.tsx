// Home placeholder. Frontend flow (Section 7, step 8) not implemented yet.
export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 32, maxWidth: 720 }}>
      <h1>Loyalty &amp; Cashback Optimizer</h1>
      <p>
        Scaffold. The rewards engine (<code>lib/rewards-engine.ts</code>) is
        fully implemented and unit-tested. API routes and UI are stubbed and
        return <code>501 Not Implemented</code> with pointers to the governing
        spec section. See <code>README.md</code> for what is verified vs. pending.
      </p>
    </main>
  );
}
